# Runtime DB user (used by spring.datasource)
SPRING_POSTGRES_USER=healenium_app
SPRING_POSTGRES_PASSWORD=healenium_app_password

# Liquibase admin user (optional, if you later separate liquibase credentials)
SPRING_LIQUIBASE_USER=healenium_user
SPRING_LIQUIBASE_PASSWORD=YDk2nmNs4s9aCP6K
